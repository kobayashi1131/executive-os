# AI業務管理OS 共同編集プロジェクト 経緯メモ

## 背景・目的

社長（鷲野さん）がClaudeとの会話で作った「AI業務管理OS」（案件管理アプリ）を、社長と小林の二人が編集できる状態にする。

- 本物のアプリ：`https://washinokensetsu.pages.dev/`（Cloudflare Pagesで公開中）
- 中身は1枚のHTMLファイル、バックエンドはGoogle Apps Script + Google Sheets
- 社長はコード編集初心者。元々Claudeとの会話だけでアプリを作っていた（コードは書かない）

## 分かったこと（事実）

- 社長の実際のGitHubアカウントを確認したところ、リポジトリの中身は空だった
- アプリを作る際に使ったのはClaudeとCloudflareのみ、GitHubは使っていない
- 社長が「アプリ内のGit」と言っていたのは、Cloudflareのデプロイ履歴（バージョン管理っぽい機能）のことだった可能性が高い。ただしこれは一人用の巻き戻し機能であり、二人での共同編集には使えない
- アプリのコード内には、`GAS_URL`（認証なしで案件データの読み書き・削除ができるエンドポイント）と、スタッフの実メールアドレスがそのまま埋め込まれている

## やったこと（今日までの作業）

1. 社長からもらったアプリのHTMLを `work/washinokensetsu/04_system/index.html` に保存し、独立したgitリポジトリとして初期化
2. 小林のGitHubに `https://github.com/kobayashi1131/test`（Private）を作成し、push
3. 小林のCloudflareアカウントでこのリポジトリとGit連携し、実際に動くデプロイ（`https://test.s-koba-cc3.workers.dev`）を確認済み
4. 社長向けのセットアップ手順（`ONBOARDING.md`、リポジトリ全体のルートに配置）を用意
   - 社長はコードを書かず、今まで通りClaudeに頼んで更新済みのコードをもらい、GitHubのWeb画面（Add file → Upload files）でアップロード＆Commitするだけの運用にする方針
   - 小林はClaude Codeで編集し、確認の上push

## 今後の方針（決まったこと）

- 仮で作った `kobayashi1131/test` は「練習」ではなく、そのまま本番の共同編集場所として使う（移行済みという位置づけ）
- 本物の `washinokensetsu.pages.dev` の接続先（Source）を、社長のCloudflare側で `kobayashi1131/test` に向け直してもらう（これは社長のアカウントでしかできない操作）
- 社長にはGitHubのコラボレーターとして招待を送る（社長のGitHubアカウント名 or メールアドレスが必要。招待画面でメール検索がヒットしない場合は、社長のプロフィールURL（`github.com/ユーザー名`）から正確なユーザー名を確認する）
- Dropbox・OneDriveのファイル共有・同期は、今回の目的（編集したら自動でアプリに反映される）には向かないと判断（Cloudflareが連携できるのはGitのみ。同期方式だと上書き事故のリスクもある）のため採用しない

## 残っているタスク

- [ ] 社長のGitHubユーザー名・メールアドレスを確定し、`kobayashi1131/test` に招待を送る
- [ ] 社長にCloudflare側で `washinokensetsu.pages.dev` の接続先を `kobayashi1131/test` に変更してもらう
- [ ] 社長にClaude Codeをセットアップしてもらい、このリポジトリに接続する（`ONBOARDING.md`参照）
- [ ] （注意）このリポジトリで「案件登録・削除」ボタンを実際に押すと、本番のGoogle Sheetsデータに影響するため、動作確認時は要注意

## 運用ルール

- pushや公開設定の変更など、外部に影響する操作は、その都度小林に確認を取ってから実行する
